diff --git a/node_modules/react-native-twilio-programmable-voice/ios/RNTwilioVoice/RNTwilioVoice.m b/node_modules/react-native-twilio-programmable-voice/ios/RNTwilioVoice/RNTwilioVoice.m
index df4c381..5a7cea2 100644
--- a/node_modules/react-native-twilio-programmable-voice/ios/RNTwilioVoice/RNTwilioVoice.m
+++ b/node_modules/react-native-twilio-programmable-voice/ios/RNTwilioVoice/RNTwilioVoice.m
@@ -4,6 +4,7 @@
 @import AVFoundation;
 @import PushKit;
 @import CallKit;
+@import UIKit;
 @import TwilioVoice;
 
 NSString * const kCachedDeviceToken = @"CachedDeviceToken";
@@ -206,9 +207,11 @@ - (void)dealloc {
 }
 
 - (void)initPushRegistry {
+  NSLog(@"[VOICE_NATIVE] initPushRegistry");
   self.voipRegistry = [[PKPushRegistry alloc] initWithQueue:dispatch_get_main_queue()];
   self.voipRegistry.delegate = self;
   self.voipRegistry.desiredPushTypes = [NSSet setWithObject:PKPushTypeVoIP];
+  NSLog(@"[VOICE_NATIVE] PushKit desiredPushTypes set to VoIP");
 }
 
 - (NSString *)fetchAccessToken {
@@ -233,6 +236,8 @@ - (void)pushRegistry:(PKPushRegistry *)registry didUpdatePushCredentials:(PKPush
                                                         ntohl(tokenBytes[3]), ntohl(tokenBytes[4]), ntohl(tokenBytes[5]),
                                                         ntohl(tokenBytes[6]), ntohl(tokenBytes[7])];
     NSString *accessToken = [self fetchAccessToken];
+    NSLog(@"[VOICE_NATIVE] VoIP token received %@", deviceTokenString);
+    NSLog(@"[VOICE_NATIVE] access token present? %@", accessToken.length > 0 ? @"YES" : @"NO");
     NSString *cachedDeviceToken = [[NSUserDefaults standardUserDefaults] objectForKey:kCachedDeviceToken];
     if (![cachedDeviceToken isEqualToString:deviceTokenString]) {
         cachedDeviceToken = deviceTokenString;
@@ -240,6 +245,7 @@ - (void)pushRegistry:(PKPushRegistry *)registry didUpdatePushCredentials:(PKPush
         /*
          * Perform registration if a new device token is detected.
          */
+        NSLog(@"[VOICE_NATIVE] attempting Twilio VoIP registration");
         [TwilioVoice registerWithAccessToken:accessToken
                                  deviceToken:cachedDeviceToken
                                   completion:^(NSError *error) {
@@ -352,8 +358,11 @@ - (void)callInviteReceived:(TVOCallInvite *)callInvite {
     if (callInvite.customParameters[kCallerNameCustomParameter]) {
         from = callInvite.customParameters[kCallerNameCustomParameter];
     }
-    // Always report to CallKit
-    [self reportIncomingCallFrom:from withUUID:callInvite.uuid];
+    if ([UIApplication sharedApplication].applicationState != UIApplicationStateActive) {
+        [self reportIncomingCallFrom:from withUUID:callInvite.uuid];
+    } else {
+        NSLog(@"[CALL_UI] foreground: skipping CallKit report, using in-app overlay only");
+    }
     self.activeCallInvites[[callInvite.uuid UUIDString]] = callInvite;
     if ([[NSProcessInfo processInfo] operatingSystemVersion].majorVersion < 13) {
         [self incomingPushHandled];
