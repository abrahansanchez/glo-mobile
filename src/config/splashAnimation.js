export const splashAnimationConfig = {
  durationMs: 1200,
  fadeOutMs: 220,
  wordmarkDelayMs: 240,
  wordmarkSlidePx: 10,
  postCycleHoldMs: 260,
  // Toggle when lottie-react-native is installed and animation json is configured.
  useLottie: false,
  // Optional bundled assets. Keep null until the files exist in-repo.
  lottieSource: null,
  gifSource: null,
};
